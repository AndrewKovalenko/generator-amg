(function(define){
  'use strict';
  define( 
         [
    'utilities/moduleFactory'{{&dependencies}}
  ], 

  function(moduleFactory){
    return moduleFactory.createNewModule({
      type: '{{moduleType}}',
      dependencies: Array.prototype.slice.call(arguments, 1, arguments.length),
      name: 'app.{{pluralModuleType}}'
    });
  });
})(window.define);
