(function(define) {
  define(function() {
    return{
      'angular': 'libs/vendors/angular/angular',
      'angularUiRouter': 'libs/vendors/angular-ui-router/release/angular-ui-router'<%=pathAliases%>
    };
  });
  //NOTE use "common" instead of "window" here 
  //to substitute "global" when reading configuration from node
}(window.define));
